
export const guards = [
    "Hank the tank",
    "Thomas the tank engine",
    "CEPUS"
];

export const addGuard = (guard) => {
    guards.push(guard);
    return guards;
}

/* export default {
    guards,
    addGuards
} */